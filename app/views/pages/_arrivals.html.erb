<div class="row font-weight-bold mb-2">
  <div class="col"><strong>Room</strong></div>
  <div class="col"><strong>Check-In</strong></div>
  <div class="col"><strong>Guest</strong></div>
</div>
<% @arrivals.each do |arrival| %>
  <div class="row arrivals mb-1">
    <div class="col"><%= arrival.room.name %></div>
    <div class="col"><%= arrival.start_date.strftime('%d %b') %></div>
    <div class="col"><%= arrival.user.first_name %> <%= arrival.user.last_name %></div>
  </div>
<% end %>
