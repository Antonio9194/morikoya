<% @rooms.each do |room| %>
  <h3><%= room.name %></h3>
  <div
    data-controller="calendar"
    data-calendar-bookings-value="<%= room.bookings.to_json(only: [:start_date, :end_date, :title]) %>">
  </div>
<% end %>
